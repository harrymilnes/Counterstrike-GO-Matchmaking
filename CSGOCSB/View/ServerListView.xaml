<UserControl x:Class="CSGOMM.View.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="599.153" d:DesignWidth="916.525">
    <StackPanel>
        <ItemsControl ItemsSource="{Binding AllServers}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Canvas>
                        <Button Content="{Binding Country}" HorizontalAlignment="Left" Background="Transparent" BorderThickness="2" BorderBrush="{Binding BlockedColourBrush}" Foreground="White" Margin="{Binding ButtonMarginString}" VerticalAlignment="Top" Width="64" Height="24" Command="{Binding ServerSelectCommand}" CommandParameter="{Binding}"/>
                        <Label Content="{Binding Ping}" HorizontalAlignment="Left" Background="Transparent" Foreground="White" Margin="{Binding LabelMarginString}" VerticalAlignment="Top" Width="64" Height="30"/>
                    </Canvas>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="205,520,0,0">
                <Button Content="Ping Servers" HorizontalAlignment="Left" Background="Transparent" BorderThickness="2" BorderBrush="DodgerBlue" Foreground="White"  Width="80" Height="24" Margin="20,0,20,0" Command="{Binding PingAllCommand}"/>
                <Button Content="Apply" HorizontalAlignment="Left" Background="Transparent" BorderThickness="2" BorderBrush="DodgerBlue" Foreground="White" Width="80" Height="24" Margin="20,0,20,0" Command="{Binding ApplyCommand}"/>
                <Button Content="Reset" HorizontalAlignment="Left" Background="Transparent" BorderThickness="2" BorderBrush="DodgerBlue" Foreground="White" Width="80" Height="24" Margin="20,0,20,0" Command="{Binding ResetCommand}"/>
                <Button Content="About" HorizontalAlignment="Left" Background="Transparent" BorderThickness="2" BorderBrush="DodgerBlue" Foreground="White" Width="80" Height="24" Margin="20,0,20,0" Command="{Binding AboutCommand}"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
